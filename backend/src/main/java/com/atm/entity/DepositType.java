package com.atm.entity;

public enum DepositType {
    /** Fixed deposit - lump-sum locked for a fixed tenure. */
    FIXED,
    /** Recurring deposit - fixed monthly contribution for a tenure. */
    RECURRING
}
