spring:
  datasource:
    url: jdbc:postgresql://db:5432/atm
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:admin123}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:8081}
